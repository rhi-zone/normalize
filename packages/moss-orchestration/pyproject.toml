[project]
name = "moss-orchestration"
version = "0.1.0"
description = "Agent orchestration: sessions, drivers, workflows, shadow git"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "Moss Contributors" }]
keywords = ["agents", "orchestration", "sessions", "workflows"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

dependencies = [
    "moss-context",
]

[project.optional-dependencies]
git = ["gitpython>=3.1.0"]

dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "ruff>=0.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/moss_orchestration"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
