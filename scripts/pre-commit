#!/usr/bin/env bash
# Pre-commit hook: Rust checks
#
# Install: cp scripts/pre-commit .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit

set -e

echo "Running cargo check..."
cargo check --quiet

echo "Running cargo fmt check..."
cargo fmt -- --check

echo "Pre-commit checks passed!"
