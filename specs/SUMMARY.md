# Specs

Specification files for normalize's external interfaces. `openapi.json` is the OpenAPI 3.1.0 schema for the normalize HTTP API (generated from the service layer). `polyglot-types.md` documents shared type conventions used across language implementations. These files serve as the authoritative contracts for API consumers and cross-language type alignment.
