# workflows

GitHub Actions workflow definitions. `ci.yml` runs tests and clippy on pull requests. `deploy-docs.yml` builds and deploys the documentation site. `normalize.yml` runs normalize checks. `release.yml` handles crate publishing and binary releases.
