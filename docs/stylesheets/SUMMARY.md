# Stylesheets

CSS overrides for the MkDocs documentation site. `custom.css` sets the project color scheme (teal primary, amber accent) and glassmorphism visual variables used across light and dark themes. Referenced from `mkdocs.yml` via the `extra_css` configuration key.
