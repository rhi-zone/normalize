# CLI Documentation

Reference documentation for each normalize CLI command, with one file per subcommand or command group. Covers the core primitives (view, edit, analyze, rank), search (grep), structural commands (structure, rules, plans), editor integrations (daemon, serve), and utility commands (init, grammars, update, generate, test). `README.md` provides the CLI architecture overview and command index. `rank.md` documents the ranked-list commands migrated from `analyze` in 2026-03. Kept in sync with the service layer as commands are added or renamed.
