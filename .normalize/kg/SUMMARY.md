Knowledge graph artifact directory for the normalize project. Contains:
- `docs-cargo-*` — scraped and structured API documentation for key crate dependencies (serde, tokio).
- `graph-substrate-design.md` / `graph-substrate-thesis.md` — design documents for the graph substrate underlying `normalize-knowledge-graph`.

These files are consumed by `normalize-knowledge-graph` and the `normalize kg` commands.
