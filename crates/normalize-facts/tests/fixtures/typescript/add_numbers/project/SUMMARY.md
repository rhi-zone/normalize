# typescript/add_numbers — project source

Source project files for the `add_numbers` fact extraction fixture for typescript.
Contains a minimal typescript program used to test symbol, import, and call extraction.
