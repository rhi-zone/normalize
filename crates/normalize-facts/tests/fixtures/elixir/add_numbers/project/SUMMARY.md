# elixir/add_numbers — project source

Source project files for the `add_numbers` fact extraction fixture for elixir.
Contains a minimal elixir program used to test symbol, import, and call extraction.
