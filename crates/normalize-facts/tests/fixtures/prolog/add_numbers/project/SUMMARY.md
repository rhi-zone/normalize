# prolog/add_numbers — project source

Source project files for the `add_numbers` fact extraction fixture for prolog.
Contains a minimal prolog program used to test symbol, import, and call extraction.
