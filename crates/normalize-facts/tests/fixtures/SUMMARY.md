# normalize-facts — test fixtures

Per-language fixture directories for fact extraction integration tests. Each subdirectory is named
for a language and contains `project/` (source files) and `expected/` (expected JSON output).
