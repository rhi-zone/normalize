# scheme/add_numbers — project source

Source project files for the `add_numbers` fact extraction fixture for scheme.
Contains a minimal scheme program used to test symbol, import, and call extraction.
