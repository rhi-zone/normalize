import math_utils;

void main() {
    auto utils = new MathUtils();
    auto sum = utils.add(3, 5);
    writeln("Sum: ", sum);

    auto product = multiply(3, 5);
    writeln("Product: ", product);
}
