# haskell/add_numbers — project source

Source project files for the `add_numbers` fact extraction fixture for haskell.
Contains a minimal haskell program used to test symbol, import, and call extraction.
