# erlang/add_numbers — project source

Source project files for the `add_numbers` fact extraction fixture for erlang.
Contains a minimal erlang program used to test symbol, import, and call extraction.
