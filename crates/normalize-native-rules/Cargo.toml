[package]
name = "normalize-native-rules"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Native rule checks for normalize (stale-summary, check-refs, stale-docs, check-examples)"

[dependencies]
normalize-output = { path = "../normalize-output", version = "0.2.0" }
normalize-facts = { path = "../normalize-facts", version = "0.2.0" }
normalize-languages = { path = "../normalize-languages", version = "0.2.0" }
normalize-budget = { path = "../normalize-budget", version = "0.2.0" }
normalize-ratchet = { path = "../normalize-ratchet", version = "0.2.0" }
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
ignore.workspace = true
regex = "1"
glob = "0.3"
schemars = "1"
dirs = "5"
