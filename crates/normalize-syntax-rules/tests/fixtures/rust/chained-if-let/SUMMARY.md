# rust/chained-if-let fixture

Fixture files for the `rust/chained-if-let` syntax rule test. `match.rs` and `no_match.rs` verify detection; `fix.rs` and `fix.expected.rs` verify the auto-fix transform (nested if-let → chained `&&` condition).
