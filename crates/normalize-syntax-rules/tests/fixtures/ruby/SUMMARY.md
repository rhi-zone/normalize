# fixtures/ruby

Test fixtures for Ruby syntax rules.

Each subdirectory corresponds to one builtin Ruby rule and contains a `match.rb` file (expected to produce findings) and a `no_match.rb` file (expected to produce zero findings). Rules covered: `ruby/binding-pry`, `ruby/global-var`, `ruby/rescue-exception`.
