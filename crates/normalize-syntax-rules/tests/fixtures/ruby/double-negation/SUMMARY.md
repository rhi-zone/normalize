# ruby/double-negation fixture

Fixture files for the `ruby/double-negation` syntax rule test. `match.rb` uses `!!expr` for boolean coercion; `no_match.rb` uses single negation, explicit comparisons, and `.nil?` checks which are not flagged.
