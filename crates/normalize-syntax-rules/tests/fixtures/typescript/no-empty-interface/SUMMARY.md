# typescript/no-empty-interface fixture

Fixture files for the `typescript/no-empty-interface` syntax rule test. `match.ts` has empty interface declarations (`{}`); `no_match.ts` has non-empty interfaces and type aliases which are not flagged.
