# typescript/no-inferrable-types fixture

Fixture files for the `typescript/no-inferrable-types` syntax rule test. `match.ts` has explicit type annotations redundant from string/number/boolean literals; `no_match.ts` uses inferred types, non-literal values, and complex types which are not flagged.
