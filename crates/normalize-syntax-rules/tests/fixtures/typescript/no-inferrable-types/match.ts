// Redundant type annotations — inferred from literals

const name: string = "Alice";
let count: number = 0;
const flag: boolean = true;
let done: boolean = false;
const greeting: string = "hello world";
