[package]
name = "normalize-rank"
description = "Shared entity types, ranking pipeline, and table rendering for normalize rank commands"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }
schemars = "1"
