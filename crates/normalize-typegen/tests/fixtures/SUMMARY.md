# normalize-typegen/tests/fixtures

JSON Schema fixture files used by codegen tests.

Three files: `user.json` (a User object schema with id, name, optional fields), `events.json` (event/payload schemas), and `petstore.json` (OpenAPI 3.x Petstore spec). These are the input schemas fed to each backend in the snapshot tests.
