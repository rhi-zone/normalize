# normalize-grammars/src

Single-file source for the `normalize-grammars` marker crate.

`lib.rs` is intentionally empty — this crate exists only to declare Cargo dependencies. No code runs from this crate at runtime; it only participates in dependency resolution so xtask can locate grammar source directories.
