# normalize-module-resolve/src

Source for the normalize-module-resolve crate.

- `lib.rs` — re-exports `ModuleResolver`, `ImportSpec`, `ModuleId`, `Resolution`, `ResolverConfig` from `normalize-languages::traits`.
