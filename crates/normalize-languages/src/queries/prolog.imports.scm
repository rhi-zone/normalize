; Prolog imports query
; @import       — the entire :- use_module directive (for line number)
; @import.path  — the module path (whole directive text)

; :- use_module(library(lists)).
; :- use_module(module, [pred/2]).
(directive_term) @import.path @import
