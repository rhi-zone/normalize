; Typst complexity query
; @complexity — nodes that increase cyclomatic complexity
; @nesting — nodes that increase nesting depth

; Complexity nodes
(if) @complexity
(while) @complexity
(for) @complexity

; Nesting nodes
(if) @nesting
(while) @nesting
(for) @nesting
