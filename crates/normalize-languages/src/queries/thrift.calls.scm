; Thrift calls query
; @call — not applicable
;
; Thrift is an IDL (Interface Definition Language), not a general-purpose
; programming language. Thrift files define service interfaces, types, and
; constants — they do not contain function call expressions. No call
; expressions exist in the grammar.
