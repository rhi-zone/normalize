; Bash calls query
; @call — command being executed
; @call.qualifier — not applicable for bash (no method receiver concept)

; Command execution: command_name is the program/function being called
(command
  name: (command_name) @call)
