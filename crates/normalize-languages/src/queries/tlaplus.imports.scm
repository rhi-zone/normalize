; TLA+ imports query
; @import       — the entire EXTENDS statement (for line number)
; @import.path  — the module name(s) being extended

; EXTENDS Naturals, Sequences
(extends) @import.path @import
