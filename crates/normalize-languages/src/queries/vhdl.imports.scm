; VHDL imports query
; @import       — the entire use clause (for line number)
; @import.path  — the library/package path
; @import.glob  — .all wildcard marker

; use ieee.std_logic_1164.all;
(use_clause) @import.path @import
