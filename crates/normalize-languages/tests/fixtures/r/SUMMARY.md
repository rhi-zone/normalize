# fixtures/r

R fixture file for `.scm` query tests.

- `sample.r` — loads `stats` and `utils` libraries, defines functions `classify`/`sum_evens`/`count_occurrences`/`factorial_r` using `<-` assignment with `function()`, includes if/else and for loops.
