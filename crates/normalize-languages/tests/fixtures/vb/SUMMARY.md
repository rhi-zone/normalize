# normalize-languages/tests/fixtures/vb

Sample Visual Basic .NET source file used by `query_fixtures.rs` to exercise all five query types (tags, calls, complexity, imports, types).

- `sample.vb` — representative Visual Basic .NET program with functions, types, imports, and control flow.
