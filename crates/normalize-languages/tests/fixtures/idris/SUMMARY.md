# normalize-languages/tests/fixtures/idris

Sample Idris source file used by `query_fixtures.rs` to exercise all five query types (tags, calls, complexity, imports, types).

- `sample.idr` — representative Idris program with functions, types, imports, and control flow.
