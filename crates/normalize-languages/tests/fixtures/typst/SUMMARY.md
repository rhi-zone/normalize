# fixtures/typst

Typst fixture file for `.scm` query tests.

- `sample.typ` — imports `template.typ` and `@preview/tablex`; defines `project_name`/`version` let bindings and `format_version`/`summary_table` function let bindings; uses `#set` and `#show` rules.
