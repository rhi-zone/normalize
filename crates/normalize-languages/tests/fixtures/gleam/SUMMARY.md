# fixtures/gleam

Gleam fixture file for `.scm` query tests.

- `sample.gleam` — imports `gleam/io`, `gleam/list`, `gleam/int`, defines `Shape` custom type, `Name` type alias, `max_size` constant, functions `classify`/`sum_evens`/`factorial`/`greet` with case expressions.
