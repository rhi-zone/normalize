# typescript/classes

Extraction fixture: two TypeScript classes (`Animal`, `Dog`) with inheritance and a standalone function (`createDog`). Verifies that class and function symbols are extracted correctly from a TypeScript source file.
