# normalize-languages/tests/fixtures/objc

Sample Objective-C source file used by `query_fixtures.rs` to exercise all five query types (tags, calls, complexity, imports, types).

- `sample.m` — representative Objective-C program with functions, types, imports, and control flow.
