# fixtures/scss

SCSS fixture file for `.scm` query tests.

- `sample.scss` — defines `flex-center`/`responsive` mixins and `rem`/`shade` functions; uses `@use`/`@import`/`@forward`; rule sets for `.container`, `.button`, and `.card` with nested selectors.
