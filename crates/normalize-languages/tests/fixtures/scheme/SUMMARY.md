# normalize-languages/tests/fixtures/scheme

Sample Scheme source file used by `query_fixtures.rs` to exercise all five query types (tags, calls, complexity, imports, types).

- `sample.scm` — representative Scheme program with functions, types, imports, and control flow.
