# fixtures/svelte

Svelte fixture file for `.scm` query tests.

- `sample.svelte` — `<script lang="ts">` with imports from `svelte` and `svelte/store`, reactive statements, `increment`/`reset` functions, lifecycle hooks; `<style>` block; template with `{#if}`/`{#each}`.
