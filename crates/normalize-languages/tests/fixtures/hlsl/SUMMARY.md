# fixtures/hlsl

HLSL fixture file for `.scm` query tests.

- `sample.hlsl` — defines `PerFrame`/`PerObject` cbuffers, `VSInput`/`PSInput` structs, and `ComputeLighting`, `VSMain`, `PSMain` functions; includes `common.hlsl` and `<d3d11.h>`.
