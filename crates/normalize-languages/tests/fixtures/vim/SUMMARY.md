# fixtures/vim

Vim script fixture file for `.scm` query tests.

- `sample.vim` — defines `ToggleOption`, `FormatBuffer`, `OpenTerminal`, and `s:LookupCache` functions; `MyPlugin` and `FileTypeSettings` augroups; `ToggleWrap`/`Term` commands; sources `utils.vim`.
