# fixtures/groovy

Groovy fixture file for `.scm` query tests.

- `sample.groovy` — imports from `groovy.transform` and `java.util`, defines `Point` and `MathUtils` classes with methods `classify`/`sumEvens`/`factorial`, and a `greet` function.
