# fixtures/erlang

Erlang fixture file for `.scm` query tests.

- `sample.erl` — module `sample` with a `point` record, `coordinate` type, functions `classify`/`sum_evens`/`factorial`/`member`/`make_point`, imports from `lists`.
