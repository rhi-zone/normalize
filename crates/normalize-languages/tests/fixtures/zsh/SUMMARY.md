# fixtures/zsh

Zsh fixture file for `.scm` query tests.

- `sample.zsh` — defines `classify`, `greet`, `sum_array`, `repeat_msg`, and `setup_dir` functions; sources two rc files; includes if/elif branching, for loops, and while loops.
