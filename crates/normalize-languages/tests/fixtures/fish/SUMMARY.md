# fixtures/fish

Fish shell fixture file for `.scm` query tests.

- `sample.fish` — defines `classify`, `greet`, `sum_list`, `repeat_msg`, and `setup_dir` functions; sources a utils file; includes if/else if branching and for loops.
