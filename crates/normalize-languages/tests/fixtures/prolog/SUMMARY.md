# fixtures/prolog

Sample Prolog source file (`sample.pl`) used by `query_fixtures.rs` to test the Prolog tree-sitter query files (tags, complexity, calls, imports). Note: `.pl` extension is shared with Perl; the grammar name is "prolog".
