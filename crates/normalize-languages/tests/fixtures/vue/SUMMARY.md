# fixtures/vue

Vue SFC fixture file for `.scm` query tests.

- `sample.vue` — `<template>` with `v-if`/`v-for`/`v-bind`/`@click`; `<script setup lang="ts">` with imports from `vue`, `vue-router`, and `@/api`; `<style scoped>` block.
