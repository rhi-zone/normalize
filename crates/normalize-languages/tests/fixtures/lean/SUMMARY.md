# normalize-languages/tests/fixtures/lean

Sample Lean 4 source file used by `query_fixtures.rs` to exercise all five query types (tags, calls, complexity, imports, types).

- `sample.lean` — representative Lean 4 program with functions, types, imports, and control flow.
