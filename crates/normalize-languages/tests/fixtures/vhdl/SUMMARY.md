# fixtures/vhdl

VHDL fixture file for `.scm` query tests.

- `sample.vhd` — defines `fifo` entity and `rtl` architecture with `mem_type` type, signals, a clocked `process`, and concurrent `assign` statements; includes `fifo_pkg` package; uses `ieee.std_logic_1164` and `ieee.numeric_std`.
