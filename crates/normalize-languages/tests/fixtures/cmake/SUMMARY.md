# fixtures/cmake

CMake fixture file for `.scm` query tests.

- `CMakeLists.txt` — defines `add_component`, `setup_target`, and `install_component` functions and a `setup_target` macro; uses `find_package(Threads)`, `find_package(OpenSSL)`, `include(GNUInstallDirs)`.
