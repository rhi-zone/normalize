# fixtures/haskell

Haskell fixture file for `.scm` query tests.

- `sample.hs` — defines a `Tree` data type, a `Count` newtype, imports from `Data.List` and `Data.Map`, functions `insert`/`member`/`classify`/`countUnique`/`frequencyMap` with guards and conditionals.
