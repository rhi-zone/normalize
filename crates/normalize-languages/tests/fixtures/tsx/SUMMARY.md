# fixtures/tsx

TypeScript/React (TSX) fixture file for `.scm` query tests.

- `sample.tsx` — defines `CounterProps`, `ButtonProps` interfaces and a `Theme` type alias, plus `Button` and `Counter` React functional components and a `classify` function; imports from `react`, `react-native`; includes JSX, useState/useEffect hooks, and if/else branching.
