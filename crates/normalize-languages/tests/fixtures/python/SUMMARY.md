# fixtures/python

Python fixture file for `.scm` query tests.

- `sample.py` — defines a `DataProcessor` class with methods, and `load_file`/`count_words` top-level functions; imports `os`, `sys`, `collections.defaultdict`, and `typing` symbols.
