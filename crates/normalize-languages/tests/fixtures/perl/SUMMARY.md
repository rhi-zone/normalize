# fixtures/perl

Perl fixture file for `.scm` query tests.

- `sample.pl` — defines a `Calculator` package with `new`, `add`, and `multiply` methods, plus top-level `classify`, `sum_array`, and `factorial` subroutines; uses `List::Util` and `POSIX` modules; includes for loops and if/elsif branching.
