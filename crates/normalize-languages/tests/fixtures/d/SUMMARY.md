# normalize-languages/tests/fixtures/d

Sample D source file used by `query_fixtures.rs` to exercise all five query types (tags, calls, complexity, imports, types).

- `sample.d` — representative D program with functions, types, imports, and control flow.
