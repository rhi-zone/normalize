# fixtures/jq

jq fixture file for `.scm` query tests.

- `sample.jq` — defines `sum`, `mean`, `flatten_keys`, `keep_if`, `count_by`, `normalize_str`, `format_record` functions; imports `lib/utils`; uses pipe expressions and `reduce`.
