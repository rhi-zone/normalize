# fixtures/meson

Meson build fixture file for `.scm` query tests.

- `meson.build` — project with `glib-2.0`, `zlib`, `threads` dependencies; `subproject('protobuf')`; defines `mylib` library, `myapp` executable, and `mylib_dep` dependency.
