# fixtures/go

Go fixture file for `.scm` query tests.

- `sample.go` — defines a `Stack` struct with `Push`/`Pop` methods, plus `Classify` and `JoinWords` top-level functions; imports `fmt` and `strings`.
