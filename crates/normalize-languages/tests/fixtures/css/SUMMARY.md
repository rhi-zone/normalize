# fixtures/css

CSS fixture file for `.scm` query tests.

- `sample.css` — demonstrates `@import "..."` and `@import url()` forms; defines CSS custom properties, `.container`, `.button` rule sets, and a media query.
