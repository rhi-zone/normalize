# normalize-languages/tests/fixtures/elm

Sample Elm source file used by `query_fixtures.rs` to exercise all five query types (tags, calls, complexity, imports, types).

- `sample.elm` — representative Elm program with functions, types, imports, and control flow.
