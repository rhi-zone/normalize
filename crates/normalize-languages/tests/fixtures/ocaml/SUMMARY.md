# fixtures/ocaml

OCaml fixture file for `.scm` query tests.

- `sample.ml` — defines a `Stack` module with a `'a t` record type, a `tree` type, functions `insert`/`classify`/`sum_evens`, opens `List` and `Printf`.
