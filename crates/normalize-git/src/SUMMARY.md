# normalize-git/src

Single-file crate library. All public API is in `lib.rs`.

- `lib.rs` — pure-Rust git read operations via `gix`: repo open, blob read, tree walk, diff (name-status and with OIDs), index status, commit history, churn stats, author shortlog, activity log, date formatting, worktree shell-out.
