[package]
name = "normalize-metrics"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Code quality metrics: cyclomatic complexity, function length, test gap analysis"

[dependencies]
normalize-analyze = { path = "../normalize-analyze", version = "0.1.0" }
normalize-facts = { path = "../normalize-facts", version = "0.1.0" }
normalize-languages = { path = "../normalize-languages", version = "0.1.0" }
normalize-output = { path = "../normalize-output", version = "0.1.0" }
serde.workspace = true
schemars = "1"
tree-sitter = "0.26"
streaming-iterator = "0.1"
nu-ansi-term = "0.50"
