//! Rule pack loading and execution - re-exported from normalize-rules.
pub use normalize_rules::{
    LoadedRulePack, RulePackError, discover, format_diagnostic, load_all, load_from_path,
    search_paths,
};
