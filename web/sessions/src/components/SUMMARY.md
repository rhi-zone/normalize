# Components

SolidJS UI components for the session log viewer. `SessionList.tsx` renders the sidebar list of available sessions, highlighting the currently selected one and emitting selection events. `ChatView.tsx` renders the selected session's log entries as a threaded chat interface, handling the various entry types (user messages, assistant messages, tool calls, summaries) with appropriate formatting.
