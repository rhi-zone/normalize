# Web Sessions Source

SolidJS source for the session log viewer. `App.tsx` defines the top-level data types (`Session`, `LogEntry`, `ContentBlock`) and manages session selection state via signals and resources. `index.tsx` is the application entry point. `markdown.ts` handles rendering log entry text as HTML. `styles.css` contains component styles. The `components/` subdirectory holds the two main UI components.
