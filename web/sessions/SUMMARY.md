# Web Sessions Viewer

A SolidJS single-page application for browsing and inspecting normalize agent session logs. Built with Vite, it renders session log entries as a chat-style interface with markdown support. The left panel lists available sessions; the right panel shows the selected session's message thread. Built output goes to `dist/`; source lives in `src/`.
