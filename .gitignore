# Documentation build
site/
docs/.vitepress/dist/
docs/.vitepress/cache/

# Normalize runtime data (see bottom for exceptions)

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
*.egg-info/
*.egg
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# build output
dist/
target/
# generated types
.astro/

# dependencies
node_modules/
bun.lockb

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# direnv
.direnv/

# database
*.sqlite
*.sqlite-shm
*.sqlite-wal

# typescript build info
*.tsbuildinfo

# vitepress cache
cache/

# debugging text files
*.txt

*.cpuprofile

# Normalize - ignore .normalize/ in subdirectories (but not root)
*/**/.normalize/

# Root .normalize/ - ignore all but config/allow files
/.normalize/*
!/.normalize/config.toml
!/.normalize/duplicate-functions-allow
!/.normalize/duplicate-blocks-allow
!/.normalize/duplicate-types-allow
!/.normalize/similar-functions-allow
!/.normalize/similar-blocks-allow
!/.normalize/hotspots-allow
!/.normalize/large-files-allow

# Normalize
.normalize/*
!.normalize/config.toml
!.normalize/duplicate-functions-allow
!.normalize/duplicate-blocks-allow
!.normalize/duplicate-types-allow
!.normalize/similar-functions-allow
!.normalize/similar-blocks-allow
!.normalize/hotspots-allow
!.normalize/large-files-allow
!.normalize/memory/
.normalize/shadow
